// Function to handle category calls to the backend. Generates url with all the category params, encodes the same and redirects the page to the url.
// function categorycall(cat1_value) {
//   if (cat1_value == "men") {
//     var cat2_value = document.getElementById("catm").value;
//   } else if (cat1_value) {
//     var cat2_value = document.getElementById("catw").value;
//   } else {
//     var cat2_value = "";
//   }

//   window.parent.location = `product-list.html?cat1=${cat1_value}&cat2=${encodeURIComponent(
//     cat2_value
//   )}`;
// }